import { css } from "@emotion/react";

const styHeaderContainer = css`
  position: fixed;
  height: 60px;
  top: 0;
  padding: 0 16px;
  z-index: 2;
  display: flex;
  align-items: center;
  background-color: #fff;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
  width: 100%;
`;

export { styHeaderContainer };
